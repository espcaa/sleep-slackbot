# sleep-slackbot

simple slack app that sends any sleep info tracked in google health to a specified slack channel with an ai roast

<img src="assets/screenshot.png" width="400"/>

ai was used to generate some boilerplate & with inline hints (copilot)
